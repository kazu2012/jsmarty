if(JSmarty.File.getSystem() != 'ajaja')
	JSmarty.File.__system__ = 'ajaja';

JSmarty.print = function(str){
	print(str);
};
JSmarty.File.fgets = function(file, dir)
{
	var text;
	return text || '';
};
JSmarty.File.mtime = function(file)
{
	
};
JSmarty.File.fputs = function(file, src)
{
	return false;
};