/**
 * JSmarty plugin
 * @package JSmarty
 * @subpackage plugins
 */

function jsmarty_modifier_date_format(string, format, default_date)
{

	if(string != '')
		return String();
	if(!default_date)
		return String();

	return '';
};